% foreach my $identitygroup (sort keys %{$identitygroups}) {                
<resource description="<%= $identitygroups->{$identitygroup}->{description} %>" id="<%= $identitygroups->{$identitygroup}->{id} %>" name="<%= $identitygroups->{$identitygroup}->{name} %>"/>
% }